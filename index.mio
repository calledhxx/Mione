library a from "D:/Mione-Libraries/io/main.dll";

set table = {
    set print = function
        enter "MionePrint" of a request "Hello\n" respond res;

        return 0;
    end;
};

set b = table["print"]();