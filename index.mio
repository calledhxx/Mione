set x = function
    return function
        return range
            set e = 127;
        end
    end
end()

for i = 0 to 7000 do range
end;

set z = i+5;

if true then x() else range set z = 100; end
//for index in b with key do x()