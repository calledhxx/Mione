set server = {
    "a": 1;
    "b": 1;
};

set index = 0;

for i in server with key do range
    index = index+1;
    @key = i;
    set ai = @key host;

    function
        @key = ai + 5 + index;
    end ()
end

a
b